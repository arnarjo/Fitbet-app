// src/components/BetReactions.tsx
import React from 'react';
import { View, Text } from 'react-native';

export default function BetReactions({ betId, userId }: { betId: string; userId: string }) {
  return null;
}
